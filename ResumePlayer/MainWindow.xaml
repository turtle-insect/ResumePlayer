<Window x:Class="ResumePlayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ResumePlayer"
        mc:Ignorable="d"
		WindowStartupLocation="CenterScreen"
		Loaded="Window_Loaded" Closed="Window_Closed"
		AllowDrop="True"
		Drop="Window_Drop"
        Title="Resume Player" Height="300" Width="500">
	<Window.DataContext>
		<local:ViewModel/>
	</Window.DataContext>
	<DockPanel>
		<Menu DockPanel.Dock="Top">
			<MenuItem Header="PlayList">
				<MenuItem Header="Append" Command="{Binding AppendPlaylistCommand}"/>
				<MenuItem Header="Clear" Command="{Binding ClearPlaylistCommand}"/>
			</MenuItem>
		</Menu>
		<StatusBar DockPanel.Dock="Top">
			<Label Content="Volume"/>
			<Slider Minimum="0" Maximum="1" SmallChange="0.01" Value="{Binding Volume}" Width="150"/>
		</StatusBar>
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition/>
				<RowDefinition Height="50"/>
			</Grid.RowDefinitions>
			<ListBox ItemsSource="{Binding Playlist.Items}"
					 SelectedIndex="{Binding Playlist.SelectIndex}"
					 HorizontalContentAlignment="Stretch">
				<ListBox.ItemTemplate>
					<DataTemplate>
						<Label Content="{Binding FileName}">
							<Label.InputBindings>
								<MouseBinding MouseAction="LeftDoubleClick"
											  Command="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=local:MainWindow}, Path=DataContext.PlayAudioCommand}"/>
							</Label.InputBindings>
						</Label>
					</DataTemplate>
				</ListBox.ItemTemplate>
			</ListBox>
			<Grid Grid.Row="1">
				<Grid.ColumnDefinitions>
					<ColumnDefinition/>
					<ColumnDefinition/>
				</Grid.ColumnDefinitions>
				<Button Content="▶️" Command="{Binding PlayAudioCommand}"/>
				<Button Grid.Column="1" Content="⏸" Command="{Binding PauseAudioCommand}"/>
			</Grid>
		</Grid>
	</DockPanel>
</Window>
